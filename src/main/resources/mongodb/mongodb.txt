#!/bin/zsh
mongo -u root -p
Maple@feng7

use carbonuser

db.createUser({
  user:"carbonuser",
  pwd:"Maple@user",
  roles:[{role:"dbOwner",db:"carbonuser"},
         {role:"readWrite",db:"mongoFile"}]
})
exit

mongo -u carbonuser -p
Maple@user
use carbonuser
db.createCollection("userImg")







